# ===========================================
# Cannabis E-commerce Application
# Environment Variables Configuration
# ===========================================
# Copy this file to .env and fill in your values
# Never commit your .env file to version control

# ===========================================
# DATABASE CONFIGURATION (Required)
# ===========================================
# Supabase PostgreSQL connection string
# Get this from: Supabase Dashboard > Settings > Database > Connection String
DATABASE_URL="postgresql://postgres.[YOUR-PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[YOUR-REGION].pooler.supabase.com:6543/postgres"

# ===========================================
# CLOUDFLARE R2 STORAGE (Required)
# ===========================================
# Get Account ID from: Cloudflare Dashboard > R2 > Overview
R2_ACCOUNT_ID="your-r2-account-id"

# Create API Token: Cloudflare Dashboard > R2 > Manage R2 API Tokens > Create API Token
R2_ACCESS_KEY_ID="your-r2-access-key"
R2_SECRET_ACCESS_KEY="your-r2-secret-key"

# Create bucket: Cloudflare Dashboard > R2 > Create bucket
R2_BUCKET_NAME="cana-products"

# Enable public access: Bucket > Settings > Public access > Allow Access
R2_PUBLIC_DOMAIN="https://pub-xxxxx.r2.dev"

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
NODE_ENV="development"
PORT="5000"

# ===========================================
# SETUP INSTRUCTIONS
# ===========================================
# 1. Database Setup (Supabase):
#    - Create project at https://supabase.com
#    - Go to Settings > Database > Connection String
#    - Copy the URI and replace [YOUR-PASSWORD] with your database password
#    - Run `npm run db:push` to create tables
#
# 2. File Storage Setup (Cloudflare R2):
#    - Create account at https://dash.cloudflare.com
#    - Go to R2 > Create bucket (name it "cana-products")
#    - Create R2 API Token with Object Read & Write permissions
#    - Enable public access on the bucket
#    - Copy the public bucket URL
